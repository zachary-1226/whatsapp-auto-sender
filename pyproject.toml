[project]
name = "whatsapp-bulk-sender"
version = "1.0.0"
description = "A bulk message sender tool for WhatsApp"
authors = [
    { name = "Zachary" }
]
dependencies = [
    "pandas",
    "selenium",
    "webdriver_manager",
    "openpyxl"
]
